import { useState } from "react";

const Sidebar = () => {
  const [showSidebar, setShowSidebar] = useState(true);

  return (
    <div className="top-0 left-0 w-[18%] bg-blue-600 fixed h-full ease-in-out duration-300
     translate-x-0">
        
    </div>  

  );
};

export default Sidebar;